apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: dittnav-api
  namespace: q1
  labels:
    team: {{team}}
spec:
  image: {{version}}
  port: 8090
  liveness:
    path: /person/dittnav-api/internal/isAlive
    initialDelay: 5
  readiness:
    path: /person/dittnav-api/internal/isReady
    initialDelay: 5
  prometheus:
    path: /person/dittnav-api/metrics
  replicas:
    min: 2
    max: 4
  webproxy: true
  ingresses:
    - "https://dittnav-api.dev-sbs.nais.io/person/dittnav-api"
    - "https://www-q1.nav.no/person/dittnav-api"
  resources:
    limits:
      cpu: 500m
      memory: 512Mi
    requests:
      cpu: 200m
      memory: 256Mi
  vault:
    enabled: true
